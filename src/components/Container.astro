---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article' | 'aside' | 'header' | 'footer' | 'nav';
}

const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class={`mx-auto w-full max-w-content px-5 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>
